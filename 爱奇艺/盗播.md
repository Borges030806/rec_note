能否将时序预测与盗播进行结合

预测目标：该uid在未来一段时间内是否可能触发高置信事件的

根据uid进行整合，uid内部按照时间顺序排列，滑动生成样本



需要构建中间表

时间粒度不同，不需要重新采样，窗口滑动的时候只需要按照一行一行滑动即可

目标标签应该是 这个用户在未来的时间段H中是否会变成黑产（而不是未来究竟会不会变成黑产，应该是一个时间段内的状态）

### 数据清洗规则：

右删失：
![[Pasted image 20260305174056.png]]


训练样本的定义：一行一个(uid, t)
![[Pasted image 20260305174231.png]]

正样本：现有数据中已经被证实是黑产
负样本：现有数据中无法证实是黑产

问题：负样本数会过多，需要处理
解决1：正样本需要全部保留，负样本按照比例抽取或者按照某种特征进行优先抽取（比如热度高的剧集优先抽取或者按照用户的活跃度进行抽取）
解决2: 采用召回-精排的思路，先通过某些特征对数据集进行召回得到候选集，再对候选集进行精排；可以把模型输出当作排序分，不一定需要真实概率，而是排序出最有可能的

问题：t的密度可能导致样本数量太多
解决：只对重要事件保留；每个用户每天选择的t的数量设置限制


其他考虑：**排序学习**、生存分析



columbus_dx.risk_log.ip
columbus_dx.risk_log.uid

策略、蜜罐、登陆ip、


**高置信：**
HIT_POT_UID_CONFLICT  账户冲突
HIT_POT_ERROR_ATK 错误应答
HIT_POT_DUPLICATE_ATK' then '重复应答'
LOST_POT 蜜罐应答缺失

**中置信：**
LT_LOST_POT 少量蜜罐应答缺失
LT_DUPLICATE_ATK 少量重复应答

**低置信(无法判断）：**
LOST_RECORD 无视频播放记录
PASS_POT 蜜罐检测正常
SKIP_POT 蜜罐未下发
UN_SUPPORT_VERSION 不支持蜜罐

登录信息表：uid、ip、dt

策略表：uid、tag、dt（日）![[Pasted image 20260306133832.png]]

蜜罐表：uid、tag、taglevel（表示tag的置信程度）、dt（日)、ptid、tv_id

播放HCDN表：可以精确到秒，没有uid
HCDN: HCDN融合了传统CDN和P2P的技术优势，提供稳定可靠、速度可控、低成本的视频文件分发网络；通过服务器和客户端SDK结合，支持着爱奇艺所有平台终端客户端APP的播放、离线下载等不同类型业务需求。
所有用户下载爱奇艺资源分片均会调用hcdn链接，该表记录了下载分片时的全脸请求信息

播放vrs表：vrs: 视频资源系统，主要负责播控；uid全为空
那么要如何把vrs表的内容和其他表中的用户信息联系起来呢

存储用户请求视频码流资源时的请求记录

理论上如果上线的话，每天新增数据，相当于利用新增数据中的uid的新样本跑一次测试（测试的过程也应该是先粗排再精排）

问题：qishu.dispatcher  --HCDN 后端调用记录 中经云的集群  [北京华为云-G1集群 ×]中的qd_uid是什么、server_ip是什么


三月底给一版模型

1. 频次特征，比如具体到某条策略的命中次数
2. 现在考虑的都是账户uid的尺度，能不能考虑换成设备的尺度
3. 用GBDT或者其他模型做过滤（可解释性可能不够）


### 加入频次特征
1. 先在魔镜里查看盗播账号和正常账号在过去30天里或者同一天里命中某条策略的次数是否有明显差异（多看几条策略）

策略例如（1763988775、
蜜罐例如（PASS_POT、
几个最近的盗播uid
![[Pasted image 20260309095700.png]]

4220830762506944

贪心算法的流程目前最后会保存前一天的l2名单的命中组合情况，但是是overwrite，也就导致没有之前的可疑uid的情况

应该看现在的l2名单里的误伤uid

![[Pasted image 20260309110425.png]]



03-08找到的combo：
(1763988775 ∩ PASS_POT ∩ HIT_POT_UID_CONFLICT) ∪ (PASS_POT ∩ HIT_POT_UID_CONFLICT) ∪ (1763988775 ∩ 1762931935 ∩ LOST_POT) ∪ (UN_SUPPORT_VERSION ∩ HIT_POT_UID_CONFLICT ∩ 1714358973) ∪ (PASS_POT ∩ UN_SUPPORT_VERSION ∩ 1714121005) ∪ (UN_SUPPORT_VERSION ∩ 1763027021 ∩ 1763972377) ∪ (PASS_POT ∩ 1763027021 ∩ LOST_RECORD) ∪ (1763988775 ∩ HIT_POT_UID_CONFLICT ∩ 1763986851) ∪ (1763027021 ∩ LOST_POT ∩ LT_LOST_POT) ∪ (1714121005 ∩ 1763986851) ∪ (1757474244 ∩ 1714121005 ∩ 1763972377) ∪ (HIT_POT_UID_CONFLICT ∩ 1714121005) ∪ (UN_SUPPORT_VERSION ∩ 1714296574 ∩ LT_LOST_POT) ∪ (1757474244 ∩ 1714121005) ∪ (PASS_POT ∩ UN_SUPPORT_VERSION ∩ LT_LOST_POT)
看看过去一个月黑产实际命中了哪几个组合

实际目前在用21个策略tag，有部分新tag只是粒度不同（例如是小时级还是分钟），后续使用可以考虑下两个范围的tag对比一下效果

设备级：只要这个设备出现了一次实锤的黑产账号，那就直接封禁



实现中：
**同一个 uid 在整个窗口内命中某个 term，只记一次分**


v1：只给每个term赋固定权重
![[Pasted image 20260310113835.png]]

v2: 加入一些额外权重：
1) 命中term个数 bonus
2) 命中高权重term个数 bonus
3) 命中天数 bonus
![[Pasted image 20260310115249.png]]

v3：加入连续命中天数加分

![[Pasted image 20260310121410.png]]

v4:
![[Pasted image 20260310123704.png]]

(1757474244 ∩ UN_SUPPORT_VERSION ∩ 1763027021) ∪ (1715136244 ∩ UN_SUPPORT_VERSION ∩ 1763986851) ∪ (PASS_POT ∩ LOST_RECORD ∩ 1714121005) ∪ (PASS_POT ∩ 1714121005 ∩ LT_LOST_POT) ∪ (1763988775 ∩ 1763027021 ∩ 1714121005) ∪ (1757474244 ∩ UN_SUPPORT_VERSION ∩ 1763986851) ∪ (1714121005 ∩ 1763986851) ∪ (1757474244 ∩ 1714121005 ∩ 1763972377) ∪ (HIT_POT_UID_CONFLICT ∩ 1714121005) ∪ (UN_SUPPORT_VERSION ∩ 1714296574 ∩ LT_LOST_POT) ∪ (1757474244 ∩ 1714121005)

一天内反复命中
还是说
很多天反复命中

提升tag上限到4
![[Pasted image 20260310170651.png]]

提升tag上限到5



1. 现有策略和蜜罐的信息量不足以区分黑白
2. 陷入局部最优

["1757474244","LOST_POT","1714358973"]


hcdn第一次请求和剧集上线first_online_time间隔时间，是否间隔时间过短且连续多天出现；

hcdn每次请求间隔时间方差，方差是否很小；

pingpack播放记录时间较长的uid，行为可能较白；

hcdn记录里，每个uid对应qipu_id数量，或者qipu_album_id数量，盗播的可能没有明显偏好，什么剧都会有hcdn记录？

pcw端的策略看看能不能扩展到全部端


把每条策略命中的uid、黑产uid 列出来，然后组合交集里uid的数量和黑产uid的数量，得到一个uid较少但黑产uid覆盖率较高的

  

蜜罐和策略的qipu_id也限制为近一个月的剧看看数量

计算召回率和准确率的方式：
看命中的样本里涉及的黑产uid去重个数占总黑产个数的比例


目前盗播LR模型做了几次特征上的优化，效果有提升但是比较有限，现在考虑加入登陆地点稳定性的相关特征测试下效果


没有可参考性的指标去筛选特征



大模型侧：
现在的逻辑：一次性产出筛选后的特征集合，然后对这些特征进行落表；
需要修改为：主要是筛选逻辑需要修改；当前的筛选逻辑是只使用提示词进行筛选，需要调整为根据这些候选特征的iv进行筛选，也就是说，我需要先获得所有候选特征的构造sql，然后对这些特征计算他们在候选uid中的iv（候选uid可以考虑选择his90d-his2d的l2名单的去重集合，量级在几千左右），最后根据iv进行筛选（还有一个考虑是，这里想要加上一个循环筛选的过程，也可以理解为多次筛选，但是这个点还没有考虑好具体要如何实现逻辑）

根据iv进行筛选，比如取top20或者top50
循环直接放代码里调用即可，不需要放奇智


**阶段 1：候选特征生成**
  **LLM 生成 N 个候选特征 SQL，例如 20~50 个**
  **不要求 LLM 决定最终入模，只要求 SQL 可执行、业务合理、无明显泄漏**

**阶段 2：IV 评估与筛选**
  **对每个候选特征，在 his90d~his2d 的历史 L2 候选 uid 去重集合上计算 IV**
  **样本需要带 label_is_black**
  **产出每个候选特征的 iv、覆盖率、缺失率、分箱明细、执行状态等**

**阶段 3：选中特征落表**
  **按 IV + 覆盖率 + 稳定性 + 去重规则选 Top K**
  **重新编号为 feature1 / feature2 / feature3 ...**
  **写入最终 selected feature meta 表**
  **后续 `piracy_LLM_feature.py` 消费这个 selected meta 表即可**


==奇智平台产出特征构造模版，代码内调用大模型获取模版，根据传参决定构造的是训练侧还是l2侧特征，然后渲染出两侧的sql，执行sql得到对应特征值，计算该特征的iv，排序，取top-k进行保留，反馈取舍情况给大模型，大模型根据反馈进行新一轮的生成==



盗播模型现在是实现了，大模型接收反馈产出特征池<->Babel计算iv并产出反馈，这样一个循环模式，目前还在测试和优化中