
1. 尝试了用hcdn播放记录构造特征，但因为表的量级太大，导致不太合适构造训练特征；
    
2. 根据统计数据构造了归一化特征，有一定优化，但还比较有限
    
3. 计划参考历史盗播模型文档构造其他特征
4. 接下来会结合ai继续进行优化



更新训练侧uid集合的来源，anchor_dt的选择还是按照时间窗口内最新一次命中combo的dt来选


大模型检查过去一周/一个月遗漏的黑产，对这些黑产进行特征分析，从而总结出若干新的可用额外特征

高阶特征器：结构化候选样本 → LLM特征服务 → 输出若干LLM特征 → 并回宽表 → LR/规则使用
二阶段打分器：LLM做二阶段打分器，对低分的进行额外分析，输出一个近似于最终判断的分数，目的是避免遗漏特征较少的黑产




盗播LR模型本周进度： 

盗播模型现在接入奇智，会根据提示词返回构造特征的定义和sql语句，从而为LR模型提供新特征，目前还在回溯历史数据对比效果；智能小助手，目前在本地跑通了测试调用飞书机器人的收发消息流程，后续功能还在实现中。


多agent或者特征筛选？比如利用agent进行特征筛选

产出的特征数太少了


