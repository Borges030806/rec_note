环境已经上传到集群

MM-TSFlib 提供端到端的管线与无缝接口，支持将任意开源语言模型与任意时间序列预测模型集成，实现多模态 TSF 任务。

该库能方便地探索 Time-MMD 数据集，并支持未来多模态 TSA 的进一步发展。

框架采用**端到端管线设计**，能够将**开源语言模型（LLMs）**与多种 TSF 模型进行集成。

**冻结 LLM 的参数**，仅训练附加的投影层

**先试试LLaMA3+Nonstat**
meta-llama/Meta-Llama-3-8B-Instruct

Llama3-8b模型已经下载到本地，也已经上传到集群

训练时每个样本取文本构造 prompt，经 LLM（如 LLaMA3/GPT2/BERT，冻结参数）得到文本嵌入 → 通过 MLP 投影到 pred_len 维 →（可选 avg/max/min/attention 池化）→ 正则化并与数据先验 prior_y 合成 prompt_y

- 文本：只会取预测段起点（s_end，对应未来窗口）的文本作为提示参与训练/融合。

- 历史窗口：只用时序数值（seq_len 部分）；不使用历史文本。

需改成从未来窗口区间 [s_end, s_end+pred_len) 收集每步文本，拼接/池化后作为单条提示输入；同步调整 prompt 构造与长度上限（避免过长），其余流程不变

环境已经上传，llam3也已经下载好

现在准备在实验室的3090上跑，191服务器

取预测窗口起点的文本；目前只适合单节点单变量；


正在跑四个数据集的实验，PTF和traffic的结果已经有了，MSPG和BjTT还在跑


在BjTT上的结果![[Pasted image 20251028141127.png]]

还是继续跑