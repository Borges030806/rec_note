将每个模态的特征划分为多个子块，方便精细化去噪。

**核心思想**：根据每个块的噪声程度和与文本的语义相关性动态计算去噪强度

结合去噪强度，更新跨模态注意力分数：引导模型在多模态交互中更关注重要信息，抑制噪声模态影响。

对去噪后的特征和原始特征进行对比学习：
鼓励模型学习**更干净、更判别性强的特征表示**。


**MoLAN+**：在去噪基础上进行 **跨模态注意力融合**。

缺点：
可以区分块是否为噪声，但是块本身包含多个特征，所以无法精确到具体是哪一个特征。
去噪是一种静态分块的操作，因为块是包含多个特征的全部数据，并没有考虑的时空信息的动态变化，某一个块的去噪强度是统一的。