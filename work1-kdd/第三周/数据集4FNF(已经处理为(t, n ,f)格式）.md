
traffic 和 weather为时空数据，其余为时间序列
traffic只有一个特征值，weather有多个特征值（可以选择只保留1个或多个，若保留一个则舍去其他）



- **数据处理**：
    
    - 统一时间步对齐（按原始采样粒度）
        
    - 缺失值处理（插值或填 0）
        
    - 转换成 (T, N, F) 或 (t, n, 1) 格式


FNF的多模态实现是通过额外的新闻数据（文本）输入，原始数据集本身是单模态数据集

多模态时**将新闻等补充文本信息嵌入时间序列数据**



