
**统一数据格式，时序都统一成(t,n)的npy文件，t是时间轴，n是节点**（**只保留数值时序信息**，忽略文本）

（t，n）即为t * n的矩阵 ，n为1的时候即为时间序列数据，n>1即为时空数据
整个数据集保存为一个大矩阵 (T_total, N)，训练时通过滑窗切分样本

**BjTT（数据集需要额外处理）**

**ChatTime（数据集需要额外处理，数据集还没定下来）**

**fnf（有几个单模态数据集，可以先处理）**


**在单模态时空预测baseline上跑这些只有时序的数据，看看效果**


MAE（平均绝对误差）、RMSE（均方根误差）、MAPE（平均绝对百分比误差）


多模态（文本+时序）数据集需要调格式（只保留数值时序信息，忽略文本）
**把数据集处理成 baseline 代码期望的输入格式**

先看baseline是怎么输入的

再调数据集使得格式匹配baseline的格式需求

1. **先写统一的数据预处理脚本**，把所有数据集转成 baseline 兼容的格式；
    
2. baseline 代码尽量不动，只改配置（路径、输入/预测长度、特征维度）；
    
3. 特殊情况（baseline 代码写死格式）再做小范围修改。



先处理fnf（单模态数据集 好处理）


问题：
1. BjTT数据集已经发邮件给作者申请了，但是作者还没回复
2. ChatTime里用的数据集选哪几个
3. FNF里用的数据集选哪几个(traffic、electricity、exchange、weather)

在数据预处理阶段补齐时间特征，将数据整理成 HimNet 期望的格式与目录结构，直接用现有脚本/模型跑通œ


MSPG数据集：有大量0


BjTT：
data和text分两个文件夹，data下 1 / 2 / 3 分别对应 2022 年 1 月、2 月、3 月的按时序采样数据（每 4 分钟一条）。每一个npy文件对应一个时间点的单个样本，形状（36，36，2），双通道，一条是等级数据，一条是道路强度数据。

栅格级展开：将每个时间点的 (36,36,2) 展平为 (1296, 2)；若只做单变量，取通道1得到 (1296,)。
			全时序堆叠后得到 (T, 1296)（或 (T, 1296, 2)）。
