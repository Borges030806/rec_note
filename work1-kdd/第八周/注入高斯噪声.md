向标准化之前的原始测试集中注入高斯噪声0、25%、50%

1. 高斯噪声注入比例可选0/25%/50%
2. 对测试集的原始数据进行注入（而不是标准化后的数据）
3. 噪声的随机种子暂时固定为42
4. 根据噪声比例以及噪声名称，对统一测试结果脚本输出的目录/share/home/22551220/project/out/12-12下也要有所不同，例如，高斯噪声0%，那就是默认数据集名称目录；高斯噪声25%，那就是/share/home/22551220/project/out/12-12/Gaussian25-数据集名称/

固定覆盖率20%的噪声 ，注入整个数据集，强度系数设置（0，25%，50%）
可视化添加噪声前后时序数据的变化

![[Pasted image 20251104092324.png]]
然后Information Bottleneck-guided MLPs for Robust Spatial-temporal Forecasting这篇我之前看的，它这里指的注入噪声的方式，我感觉应该是输入和标签都注入噪声吗？因为论文里之前的意思大概是因为窗口是滑动的，所以某时刻的预测目标也可能是另一时刻的输入数据，所以他觉得应该同时考虑输入数据和预测目标的噪声来验证模型的鲁棒性（所以我感觉这里应该还是仅针对测试集加噪声），这个模型RSTIB的代码里也没有进行外部加噪声，他在训练代码里有一个噪声模块是IB内部实现的一个自适应正则化（大概是利用噪声去估计“难度”，然后调整正则化强度）


1. Bernoulli 采样整个数据集，概率20%，随机种子固定为42
2. 取训练集该数值列的标准差参与后续噪声强度的计算
3. 统一生成四个数据集的噪声强度25%和50%的噪声数据集，然后各个baseline重新预处理重新跑


对整个数据集（训练集、验证集、测试集）生成每个样本的时候对hist内部的各个时间步的各个节点的数值按照Bernoulli采样20%概率注入噪声，pred保持不变


训练集的输入和输出都需要加入噪声，验证集和测试集只需要在输入端加入噪声


